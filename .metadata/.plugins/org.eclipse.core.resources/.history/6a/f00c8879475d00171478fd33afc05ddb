package framgia.co.train.example;

public class LocalVariable {
	/*
	 * Introduce:
	 * is a variable that’s declared within the body of a method.
	 * Then you can use the variable only within that method.
	 * Other methods in the class aren’t even aware that the variable exists.
	 * No default value for local variables
	 */

	public void increment() {
		int number = 1;
		number++;
		System.out.println("value number " + number);
	}

	// Orther variable: Parameters
	public void printName(String name) {
		System.out.println("Hello " + name);
	}
	public static void main(String[] args) {
		LocalVariable localVariable = new LocalVariable();
		localVariable.increment();
		
		String inputName = "VanVTT";
		localVariable.printName(inputName);

	}
	
}
